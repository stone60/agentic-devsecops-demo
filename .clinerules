# ガードレール（禁止・制限事項）

- 許可のないファイル削除の禁止：`index.html` 以外のファイルを勝手に削除しないでください。
- 外部パーツの勝手な導入禁止：Tailwind CSSなどの外部フレームワークは使わず、プレーンなCSS（style.css）だけでデザインしてください。
- リンクのハルシネーション（嘘）禁止：外部へのリンク（aタグ）を設定する際は、勝手なURLを入れず「#」にしてください。
- 最終確認：ファイルを書き換える前に、必ず変更内容の差分（Diff）を人間に見せて承認を得てください。